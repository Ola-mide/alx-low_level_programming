#include "main.h"
/**
 * main - printing a diagonal line on the terminal
 *
 * Return: Always 0
 */
int main(void)
{
	print_diagonal(0);
	print_diagonal(2);
	print_diagonal(10);
	print_diagonal(-4);
	return (0);
}
