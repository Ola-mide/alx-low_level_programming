#include "main.h"
#include <stdlib.h>

/**
 * _abs - a function that computes the absolute value of an integer
 *
 * @a: absolute value
 *
 * Return: returns absolute value
 */

int _abs(int a)
{
	int absolute = abs(a);

	return (absolute);
}
