#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string
 *
 * @s: string passed to function
 *
 * Return: Always 0
 */
int _strlen(char *s)
{
	int a;

	a = strlen(s);
	return (a);
}
