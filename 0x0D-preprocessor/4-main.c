#include <stdio.h>
#include "4-sum.h"
#include "4-sum.h"

/**
 * main - using a function like macro that finds the sum of two numbers
 *
 * Return: Always 0.
 */
int main(void)
{
    int s;

    s = SUM(98, 1024);
    printf("%d\n", s);
    return (0);
}
